<%= sidebar_section("account", workspace_header, true) do %>
  <ul class="nav-list">
    <% if current_user %>
      <li><a href="<%= eshelf_url %>?institution=<%= current_primary_institution.code unless current_primary_institution.nil? %>">My e-shelf</a></li>
      <li><a href="<%= my_account_url %>?institution=<%= current_primary_institution.code unless current_primary_institution.nil? %>">My library account</a></li>
      <li><%= link_to "Log-out #{current_user.firstname}", logout_url, :class=>"logout" %></li>
    <% else %>
      <li><a href="<%= eshelf_url %>?institution=<%= current_primary_institution.code unless current_primary_institution.nil? %>">Guest e-shelf (log in for more options)</a></li>
    <% end %>
  </ul>
<% end %>
<%= render :partial => "#{current_primary_institution.views["dir"]}/sidebar" unless current_primary_institution.nil? %>
